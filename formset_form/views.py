from django.shortcuts import render
from .forms import FormsetFormForm
# Create your views here.
def index(request):
    form
    return render(request, 'formset_form/form.html', {
        'form': form
    })